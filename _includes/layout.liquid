<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body>
  <h1>{{ title }}</h1>
  
  {% if body %}
    {% for block in body %}
      {% assign blockType = block.template %}
      {% case blockType %}
        {% when 'text' %}
          <p>
            {{ block.text }}
          </p>
        {% when 'image' %}
          <figure>
            <img src="{{ block.image|first }}" alt="image">
          </figure>
      {% endcase %}
    {% endfor %}
  {% endif %}
</body>
</html>